export default function Banner() {
    //if (props.origin === "home") {
      return (
        <section className="banner_home">
          <h2>Chez vous, partout et ailleurs</h2>
        </section>
      );
    //} else if (props.origin === "apropos") {
   //   return <section className="banner_about"></section>;
    }
  
  
